#include <bits/stdc++.h>
using namespace std;

priority_queue<int, vector<int>> pq;


int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int tt;
	cin >> tt;
	while (tt--) {
		int x, y;
		cin >> x >> y;
		for (int i = 1; i <= y; i++) {
			int a, b;
			cin >> a >> b;
		}

		cout << x - 1 << '\n';
	}
}